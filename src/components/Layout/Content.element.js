import styled, {css} from "styled-components";

export const ContentStyle = styled.div`
    cursor: pointer;
    background-color: orange;
    &:hover {
        transform: translate(0.2rem, -0.5ex);
    }
`;